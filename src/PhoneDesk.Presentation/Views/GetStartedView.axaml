<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fi="using:FluentIcons.Avalonia"
             xmlns:loc="clr-namespace:PhoneDesk.Localization"
             xmlns:local="clr-namespace:PhoneDesk.Views"
             xmlns:viewmodels="clr-namespace:PhoneDesk.ViewModels"
             xmlns:converters="clr-namespace:PhoneDesk.Converters"
             x:Class="PhoneDesk.Views.GetStartedView"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <!-- DataContext is set via data binding from MainWindow -->

    <UserControl.Resources>
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <UserControl.Styles>
        <Style Selector="Border.setup-guidance">
            <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AccentHairlineBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="14"/>
        </Style>
    </UserControl.Styles>

    <ScrollViewer Name="ReadinessScroll" HorizontalScrollBarVisibility="Disabled" Padding="0,0,0,32">
        <StackPanel Margin="32" MaxWidth="850" HorizontalAlignment="Center" Spacing="24">

            <!-- Header Section -->
            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="14" Margin="0,0,0,16">
                <Border Grid.Column="0" Classes="icon-badge" VerticalAlignment="Top">
                    <fi:SymbolIcon Symbol="Rocket"/>
                </Border>
                <StackPanel Grid.Column="1" Spacing="4" VerticalAlignment="Center">
                    <TextBlock Text="{loc:Translate Key=GetStartedTitle}"
                             Classes="page-title"
                             Margin="0"/>
                    <TextBlock Text="{loc:Translate Key=GetStartedSubtitle}"
                             Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                             TextWrapping="Wrap"/>
                </StackPanel>
            </Grid>

            <Border Classes="setup-guidance">
                <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                    <fi:SymbolIcon Grid.Column="0"
                                   Symbol="Info"
                                   FontSize="22"
                                   Foreground="{DynamicResource TeamsPrimaryBrush}"
                                   VerticalAlignment="Top"/>
                    <StackPanel Grid.Column="1" Spacing="4">
                        <TextBlock Text="{loc:Translate Key=GetStartedWhatHappensHereTitle}"
                                   FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding SetupGuidance}"
                                   TextWrapping="Wrap"/>
                        <TextBlock Text="{Binding SetupGuidanceDetail}"
                                   FontSize="12"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                    <Button Grid.Column="2"
                            Command="{Binding NavigateToPageCommand}"
                            CommandParameter="Variables"
                            IsVisible="{Binding CanProceed}"
                            Classes="accent IconText"
                            VerticalAlignment="Center"
                            MinWidth="190">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBlock Text="{loc:Translate Key=GetStartedStartConfigurationAction}" VerticalAlignment="Center"/>
                            <fi:SymbolIcon Symbol="ArrowRight"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>

            <Expander Name="SetupChecklist" HorizontalAlignment="Stretch"
                      Header="{loc:Translate Key=GetStartedChecklistTitle}">
                <StackPanel Spacing="12" Margin="12">
                    <TextBlock Text="{loc:Translate Key=GetStartedChecklistAccount}" TextWrapping="Wrap"/>
                    <TextBlock Text="{loc:Translate Key=GetStartedChecklistPlan}" TextWrapping="Wrap"/>
                    <TextBlock Text="{loc:Translate Key=GetStartedChecklistScope}" TextWrapping="Wrap"/>
                </StackPanel>
            </Expander>

            <!-- Step 1: PowerShell Modules -->
            <Border Classes="card interactive" AutomationProperties.Name="{Binding ModulesStatusText}">
                <Grid ColumnDefinitions="Auto,*,Auto" Margin="8">
                    <!-- Step Indicator -->
                    <Panel Grid.Column="0" Width="32" Height="32" VerticalAlignment="Center" Margin="0,0,24,0"
                           AutomationProperties.Name="{Binding ModulesStatusText}"
                           AutomationProperties.LiveSetting="Polite"
                           ToolTip.Tip="{Binding ModulesStatusText}">
                        <!-- Pending State: Ring with Number -->
                        <Border CornerRadius="16"
                                BorderBrush="{DynamicResource AccentHairlineBrush}"
                                BorderThickness="2"
                                IsVisible="{Binding !ModulesChecked}"
                                AutomationProperties.Name="{loc:Translate Key=GetStartedStep1PendingA11y}"
                                ToolTip.Tip="{loc:Translate Key=GetStartedStep1PendingA11y}">
                            <TextBlock Text="1" 
                                     FontSize="15"
                                     FontWeight="Bold"
                                     FontFamily="{StaticResource DisplayFontFamily}"
                                     Foreground="{DynamicResource TeamsPrimaryBrush}"
                                     HorizontalAlignment="Center"
                                     VerticalAlignment="Center"/>
                        </Border>
                        <!-- Completed State: Clean Icon -->
                        <fi:SymbolIcon Symbol="CheckmarkCircle" 
                                     FontSize="28"
                                     HorizontalAlignment="Center"
                                     VerticalAlignment="Center"
                                     Foreground="{DynamicResource SystemFillColorSuccessBrush}"
                                     IsVisible="{Binding ModulesChecked}"
                                     AutomationProperties.Name="{loc:Translate Key=GetStartedStep1CompletedA11y}"
                                     ToolTip.Tip="{loc:Translate Key=GetStartedStep1CompletedA11y}"/>
                    </Panel>
                    <!-- Content -->
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="6">
                        <TextBlock Text="{loc:Translate Key=GetStartedModulesTitle}"
                                 FontSize="18"
                                 FontWeight="SemiBold"/>
                        <TextBlock Text="{loc:Translate Key=GetStartedModulesDescription}"
                                 FontSize="14"
                                 Opacity="0.7"
                                 TextWrapping="Wrap"/>
                    </StackPanel>
                    <!-- Action -->
                    <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="24,0,0,0">
                        <Button Command="{Binding CheckModulesCommand}"
                                IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}"
                                Classes="IconText"
                                MinWidth="160">
                                <StackPanel Orientation="Horizontal">
                                    <fi:SymbolIcon Symbol="BoxToolbox"/>
                                <TextBlock Text="{Binding ModulesActionText}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Step 2: Teams Connection -->
            <Border Classes="card interactive" AutomationProperties.Name="{Binding TeamsStatusText}">
                <Grid ColumnDefinitions="Auto,*,Auto" Margin="8">
                    <!-- Step Indicator -->
                    <Panel Grid.Column="0" Width="32" Height="32" VerticalAlignment="Center" Margin="0,0,24,0"
                           AutomationProperties.Name="{Binding TeamsStatusText}"
                           AutomationProperties.LiveSetting="Polite"
                           ToolTip.Tip="{Binding TeamsStatusText}">
                        <Border CornerRadius="16"
                                BorderBrush="{DynamicResource AccentHairlineBrush}"
                                BorderThickness="2"
                                IsVisible="{Binding !TeamsConnected}"
                                AutomationProperties.Name="{loc:Translate Key=GetStartedStep2PendingA11y}"
                                ToolTip.Tip="{loc:Translate Key=GetStartedStep2PendingA11y}">
                            <TextBlock Text="2" 
                                     FontSize="15"
                                     FontWeight="Bold"
                                     FontFamily="{StaticResource DisplayFontFamily}"
                                     Foreground="{DynamicResource TeamsPrimaryBrush}"
                                     HorizontalAlignment="Center"
                                     VerticalAlignment="Center"/>
                        </Border>
                        <fi:SymbolIcon Symbol="CheckmarkCircle" 
                                     FontSize="28"
                                     HorizontalAlignment="Center"
                                     VerticalAlignment="Center"
                                     Foreground="{DynamicResource SystemFillColorSuccessBrush}"
                                     IsVisible="{Binding TeamsConnected}"
                                     AutomationProperties.Name="{loc:Translate Key=GetStartedStep2CompletedA11y}"
                                     ToolTip.Tip="{loc:Translate Key=GetStartedStep2CompletedA11y}"/>
                    </Panel>
                    <!-- Content -->
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="6">
                        <TextBlock Text="{loc:Translate Key=GetStartedTeamsTitle}"
                                 FontSize="18"
                                 FontWeight="SemiBold"/>
                        <TextBlock Text="{loc:Translate Key=GetStartedTeamsDescription}"
                                 FontSize="14"
                                 Opacity="0.7"
                                 TextWrapping="Wrap"/>
                    </StackPanel>
                    <!-- Action -->
                    <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="24,0,0,0">
                        <Button Command="{Binding ConnectTeamsCommand}"
                                IsVisible="{Binding !TeamsConnected}"
                                IsEnabled="{Binding CanConnectTeams}"
                                Classes="IconText"
                                MinWidth="160">
                            <StackPanel Orientation="Horizontal">
                                <fi:SymbolIcon Symbol="Person"/>
                                <TextBlock Text="{Binding TeamsActionText}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding DisconnectTeamsCommand}"
                                IsVisible="{Binding TeamsConnected}"
                                Classes="IconText"
                                Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                                MinWidth="160">
                            <StackPanel Orientation="Horizontal">
                                <fi:SymbolIcon Symbol="SignOut"/>
                                <TextBlock Text="{loc:Translate Key=GetStartedDisconnectAction}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Step 3: Graph Connection -->
            <Border Classes="card interactive" AutomationProperties.Name="{Binding GraphStatusText}">
                <Grid ColumnDefinitions="Auto,*,Auto" Margin="8">
                    <!-- Step Indicator -->
                    <Panel Grid.Column="0" Width="32" Height="32" VerticalAlignment="Center" Margin="0,0,24,0"
                           AutomationProperties.Name="{Binding GraphStatusText}"
                           AutomationProperties.LiveSetting="Polite"
                           ToolTip.Tip="{Binding GraphStatusText}">
                        <Border CornerRadius="16"
                                BorderBrush="{DynamicResource AccentHairlineBrush}"
                                BorderThickness="2"
                                IsVisible="{Binding !GraphConnected}"
                                AutomationProperties.Name="{loc:Translate Key=GetStartedStep3PendingA11y}"
                                ToolTip.Tip="{loc:Translate Key=GetStartedStep3PendingA11y}">
                            <TextBlock Text="3" 
                                     FontSize="15"
                                     FontWeight="Bold"
                                     FontFamily="{StaticResource DisplayFontFamily}"
                                     Foreground="{DynamicResource TeamsPrimaryBrush}"
                                     HorizontalAlignment="Center"
                                     VerticalAlignment="Center"/>
                        </Border>
                        <fi:SymbolIcon Symbol="CheckmarkCircle" 
                                     FontSize="28"
                                     HorizontalAlignment="Center"
                                     VerticalAlignment="Center"
                                     Foreground="{DynamicResource SystemFillColorSuccessBrush}"
                                     IsVisible="{Binding GraphConnected}"
                                     AutomationProperties.Name="{loc:Translate Key=GetStartedStep3CompletedA11y}"
                                     ToolTip.Tip="{loc:Translate Key=GetStartedStep3CompletedA11y}"/>
                    </Panel>
                    <!-- Content -->
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="6">
                        <TextBlock Text="{loc:Translate Key=GetStartedGraphTitle}"
                                 FontSize="18"
                                 FontWeight="SemiBold"/>
                        <TextBlock Text="{loc:Translate Key=GetStartedGraphDescription}"
                                 FontSize="14"
                                 Opacity="0.7"
                                 TextWrapping="Wrap"/>
                    </StackPanel>
                    <!-- Action -->
                    <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="24,0,0,0">
                         <Button Command="{Binding ConnectGraphCommand}"
                                IsVisible="{Binding !GraphConnected}"
                                IsEnabled="{Binding CanConnectGraph}"
                                Classes="IconText"
                                MinWidth="160">
                            <StackPanel Orientation="Horizontal">
                                <fi:SymbolIcon Symbol="Cloud"/>
                                <TextBlock Text="{Binding GraphActionText}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding DisconnectGraphCommand}"
                                IsVisible="{Binding GraphConnected}"
                                Classes="IconText"
                                Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                                MinWidth="160">
                            <StackPanel Orientation="Horizontal">
                                <fi:SymbolIcon Symbol="SignOut"/>
                                <TextBlock Text="{loc:Translate Key=GetStartedDisconnectAction}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Proceed Section -->
            <Border Classes="card"
                    Background="{DynamicResource SuccessSoftBrush}"
                    BorderBrush="{DynamicResource SuccessHairlineBrush}"
                    Margin="0,16,0,0"
                    IsVisible="{Binding CanProceed}">
                <Border.Styles>
                    <Style Selector="Border">
                        <Style.Animations>
                            <Animation Duration="0:0:0.3">
                                <KeyFrame Cue="0%">
                                    <Setter Property="Opacity" Value="0"/>
                                </KeyFrame>
                                <KeyFrame Cue="100%">
                                    <Setter Property="Opacity" Value="1"/>
                                </KeyFrame>
                            </Animation>
                        </Style.Animations>
                    </Style>
                </Border.Styles>
                <Grid ColumnDefinitions="*,Auto" ColumnSpacing="16" Margin="8">
                    <StackPanel Grid.Column="0" VerticalAlignment="Center" Spacing="4">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <fi:SymbolIcon Symbol="CheckmarkCircle" Foreground="{DynamicResource SystemFillColorSuccessBrush}" FontSize="24"/>
                            <TextBlock Text="{loc:Translate Key=GetStartedAllSystemsReadyTitle}"
                                     FontSize="18"
                                     FontWeight="SemiBold"/>
                        </StackPanel>
                        <TextBlock Text="{loc:Translate Key=GetStartedAllSystemsReadyDescription}" TextWrapping="Wrap"
                                 FontSize="14"
                                 Opacity="0.7"/>
                    </StackPanel>
                    <Button Grid.Column="1"
                            Command="{Binding NavigateToPageCommand}"
                            CommandParameter="Variables"
                            Classes="accent IconTextLarge"
                            FontWeight="Bold"
                            MinWidth="200">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <TextBlock Text="{loc:Translate Key=GetStartedStartConfigurationAction}" VerticalAlignment="Center"/>
                            <fi:SymbolIcon Symbol="ArrowRight"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>

            <!-- Loading Indicator -->
             <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Center" 
                        Spacing="12"
                        IsVisible="{Binding IsBusy}">
                <ProgressBar IsIndeterminate="True" Width="20" Height="20"/>
                <TextBlock Text="{loc:Translate Key=GetStartedProcessing}" Opacity="0.7"/>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</UserControl>
